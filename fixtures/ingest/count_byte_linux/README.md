# Ingest fixture — `count_byte` Linux (sysv64)

Mirror of `fixtures/ingest/count_byte/` for `x86_64-unknown-linux-gnu`.

Used by Gate-1/2 Linux CI smokes. SemASM pin is the VAA workflow SHA; this is a
VAA+SemASM smoke on that pin, not a claim that SemASM Linux assemble/link is
upstream CI-verified.
