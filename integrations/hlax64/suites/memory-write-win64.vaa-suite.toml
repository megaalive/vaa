# Phase D â€” memory writes (Win64). See CORPUS.md.
schema_version = "0.1"
suite_id = "hlax64.backend.memory-write.win64.v0"
target = "x86_64-pc-windows-msvc"
abi = "win64"
stack_lock = "../stack.lock.toml"
required_cases = [
  "../cases/replace_byte",
  "../cases/memset",
  "../cases/memcpy",
]

[generator]
spec = "../generator.spec.toml"

[policy]
require_all_cases = true
allow_verified_under_preconditions = false
allow_incomplete = false
stop_on_first_failure = false
max_parallel_cases = 1
