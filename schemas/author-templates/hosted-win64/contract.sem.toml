# Placeholder contract for hosted Win64 scaffold.
# Do NOT treat the hosted REPL as verified. Admit/verify real leaves separately.
schema_version = "0.5"
routine = "__NAME___leaf_stub"
target = "__TARGET__"

[function]
abi = "__ABI__"
[function.params]
a = "i64"
[function.returns]
result = "i64"

[requires]
# Hosted programs are integration-only.

[ensures]
true

[[vectors]]
name = "stub"
a = 0
expected = 0
